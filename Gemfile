source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# Ruby 3 needs webrick for `jekyll serve` (if it isn't already pulled in)
gem "webrick"

gem "jekyll-feed", group: :jekyll_plugins